{{ form_start(form) }}

    <div class="panel panel-default">
        <div class="panel-body">

            <div class="row">
                <div class="col-lg-12">
                    {{ form_row(form.codigo) }}                
                </div>
            </div>

            <div class="row">
                <div class="col-lg-12">
                    {{ form_row(form.nome) }}                
                </div>
            </div>

            <div class="row">
                <div class="col-lg-4">
                    {{ form_row(form.dataNascimento) }}                
                </div>
                <div class="col-lg-4">
                    {{ form_row(form.email) }}                
                </div>
                <div class="col-lg-4">
                    {{ form_row(form.telefone) }}                
                </div>
            </div>

            <div class="row">
                <div class="col-lg-12">
                    {{ form_row(form.observacao) }}                
                </div>
            </div>

        </div>
    </div>

    <p><button class="btn btn-lg btn-block btn-primary">Gravar</button></p>
{{ form_end(form) }}